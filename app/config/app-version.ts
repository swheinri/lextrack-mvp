// app/config/app-version.ts

// Zentrale Stelle für die App-Version.
// Bitte nur anpassen, wenn du eine neue Version in package.json / Git taggst.
export const APP_VERSION = '0.2.0';
export const APP_VERSION_LABEL = `LexTrack v${APP_VERSION}`;
